package it.univaq.webengineering.soccorsoweb.controller;import org.springframework.stereotype.Controller;import org.springframework.ui.Model;import org.springframework.web.bind.annotation.GetMapping;import java.security.Principal;@Controllerpublic class AdminController {    @GetMapping("/admin") // O /dashboard_operatore.ftl    public String dashboard(Model model, Principal principal) {        // 1. Recupera il nome utente (es. email o username)        String username = "Ospite";        if (principal != null) {            username = principal.getName();        }        // 2. Passa i dati al template Freemarker        model.addAttribute("nomeUtente", username);        // Esempio: passa anche un ruolo o altre info        model.addAttribute("ruolo", "Amministratore");        return "admin/admin"; // Cerca admin.ftl    }    @GetMapping("/admin/aggiungi-utente")    public String nuovoOperatore(Model model) {        return "/admin/aggiungi-utente"; // Cerca nuovo_operatore.ftl    }}